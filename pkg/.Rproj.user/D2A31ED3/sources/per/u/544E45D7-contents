emmy <- fm$uhat
miray <- A[take,take] %*% Rest_term 

plot(emmy, miray)

A1 <- MASS::ginv(A)

emma <- A1 %*% fm$uhat
miraa <- A1 %*% miray

plot(emma, miraa)

emmu <- Zt %*% emma
mirau <- Zt %*% miraa

plot(emmu, mirau)
