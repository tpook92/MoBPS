## Absturz
library(RandomFieldsUtils)
library(miraculix)
RFoptions()
library(MoBPS)
pop1 <- creating.diploid(nindi=100, nsnp=1000, miraculix=TRUE, n.additive = 100)
pop2 <- breeding.diploid(pop1, breeding.size=50, selection.size=c(10,10))

RFoptions(snpcoding="ThreeBit")

pop1 <- json.simulation("C:/Users/pook/Downloads/EAAPchicken.json")
